/**
 * Design System Plugins
 *
 * Pre-built plugins that extend design system components with additional variants.
 * Import and register plugins with the PluginRegistry to enable their variants.
 */

export { premiumButtonPlugin, default as PremiumButtonPlugin } from "./premium-button.plugin";
export { statusBadgePlugin, default as StatusBadgePlugin } from "./status-badge.plugin";
